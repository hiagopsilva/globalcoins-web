import { LineChart } from '@mui/x-charts'

import styled from 'styled-components'

export const BarChartStyled = styled(LineChart).attrs({
  height: 300,
  colors: ['#ec8f21'],
})`
  font-size: 4px;
`
